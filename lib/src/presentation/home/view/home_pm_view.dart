import 'package:flutter/material.dart';

class HomePmView extends StatelessWidget {
  const HomePmView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
